{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts",
    "start": "bun install && bun run src/index.ts"
  },
  "dependencies": {
    "hono": "^3.12.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.7"
  }
}